<section class="section pexisa" id="pexisa">
  <div class="section-inner pexisa__grid">
    <div>
      <p class="eyebrow">Fabricacion y marca propia</p>
      <h2>Pexisa: basculas industriales configurables, reparables y pensadas para trabajo real.</h2>
      <p class="lead">
        Como fabricante directo, Astil Basculas puede adaptar dimensiones, capacidades y
        aplicaciones cuando las opciones estandar no encajan con la operacion del cliente.
      </p>
    </div>
    <div class="capabilities">
      <div>
        <strong>Construccion robusta</strong>
        <span>Componentes seleccionados para entornos industriales.</span>
      </div>
      <div>
        <strong>Servicio reparable</strong>
        <span>Equipos disenados para mantenerse utiles y atendibles.</span>
      </div>
      <div>
        <strong>Integracion futura</strong>
        <span>Base preparada para sistemas automatizados y software logistico.</span>
      </div>
    </div>
  </div>
</section>

<style>
  .pexisa {
    background: var(--paper);
  }

  .pexisa__grid {
    display: grid;
    grid-template-columns: minmax(0, 0.95fr) minmax(0, 1.05fr);
    gap: clamp(34px, 6vw, 84px);
    align-items: start;
  }

  .capabilities {
    display: grid;
    gap: 14px;
  }

  .capabilities div {
    padding: 22px;
    border-left: 5px solid var(--gold);
    border-radius: 8px;
    background: #ffffff;
    box-shadow: 0 10px 24px rgba(16, 24, 32, 0.07);
  }

  .capabilities strong,
  .capabilities span {
    display: block;
  }

  .capabilities span {
    margin-top: 6px;
    color: var(--muted);
  }

  @media (max-width: 860px) {
    .pexisa__grid {
      grid-template-columns: 1fr;
    }
  }
</style>
