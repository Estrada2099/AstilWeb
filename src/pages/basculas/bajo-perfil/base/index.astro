---
import SmartImage from "../../../../components/SmartImage.astro";
import { contact } from "../../../../data/site";
import BaseLayout from "../../../../layouts/BaseLayout.astro";

const base = import.meta.env.BASE_URL.endsWith("/")
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`;
const withBase = (path: string) => `${base}${path.replace(/^\//, "")}`;
const fichaHref = withBase("/downloads/FHT-BAPER-12.pdf");
const whatsappMessage = encodeURIComponent(
  "Hola, quiero cotizar la Báscula de Bajo Perfil Base BAPER-12 de 120 x 120 cm y 1,500 kg. Estoy en CDMX o Estado de México y quiero confirmar disponibilidad."
);
const whatsappHref = `${contact.whatsapp[0].href}?text=${whatsappMessage}`;
const imageSlots = {
  hero: "/Bajo-Perfil/base/Baper-hero",
  heroPlatform: "/Bajo-Perfil/base/base-producto",
  indicator: "/Bajo-Perfil/base/ipes-producto",
  surfaceDetail: "/Bajo-Perfil/base/base-beneficios",
  useIdeal: "/Bajo-Perfil/base/base-uso-ideal",
  application: "/Bajo-Perfil/base/entrega2",
  bundle: "/Bajo-Perfil/base"
};

const seo = {
  title: "Báscula de Bajo Perfil Base BAPER-12 120x120 cm 1500 kg | Entrega en CDMX y Estado de México",
  description:
    "Báscula de bajo perfil Base BAPER-12 de 120 x 120 cm, altura de 11 cm y capacidad de 1,500 kg. Ideal para almacén, recibo y uso moderado. Entrega en CDMX y Estado de México sin cargo adicional.",
  keywords:
    "BAPER-12, báscula de bajo perfil, báscula de piso, báscula industrial de piso, báscula 120 x 120 cm, báscula de 1500 kg, báscula para almacén, báscula para recibo de mercancía, báscula para embarques, báscula industrial en CDMX, báscula de piso en Estado de México"
};

const quickFacts = [
  { value: "1,500 kg", label: "Capacidad máxima" },
  { value: "120 × 120 cm", label: "Plataforma" },
  { value: "11 cm", label: "Altura" },
  { value: "Acero al carbón", label: "Material" },
  { value: "Compatible", label: "Rampa" },
  { value: "100 horas", label: "Pila recargable" }
];

const trustItems = [
  {
    title: "Diseño de bajo perfil",
    text: "Facilita operaciones de pesaje básico sin instalación compleja."
  },
  {
    title: "Superficie antiderrapante",
    text: "Ayuda a mejorar seguridad y estabilidad durante la carga."
  },
  {
    title: "Capacidad de 1,500 kg",
    text: "Adecuada para carga controlada en almacén, recibo y distribución."
  },
  {
    title: "Compatible con rampa",
    text: "Permite facilitar carga y descarga con una configuración adecuada."
  },
  {
    title: "Indicador IPES incluido",
    text: "Indicador comercial de solo peso, práctico para operación sencilla."
  },
  {
    title: "Pila hasta 100 horas",
    text: "Indicador con batería recargable para uso continuo."
  }
];

const benefitCards = [
  {
    title: "Diseño de bajo perfil",
    badge: "Acceso más práctico",
    text: "Facilita operaciones de pesaje básico sin instalación compleja."
  },
  {
    title: "Superficie antiderrapante",
    badge: "Mayor estabilidad",
    text: "Ayuda a mejorar seguridad y control durante la carga."
  },
  {
    title: "Capacidad de 1,500 kg",
    badge: "Carga controlada",
    text: "Adecuada para almacén, recibo y pequeños procesos de distribución."
  },
  {
    title: "Compatible con rampa",
    badge: "Carga y descarga",
    text: "Permite facilitar el acceso de mercancía con una configuración adecuada."
  },
  {
    title: "Indicador IPES incluido",
    badge: "Solo peso",
    text: "Indicador comercial práctico para operación sencilla y lectura clara."
  },
  {
    title: "Pila recargable",
    badge: "Hasta 100 horas",
    text: "Autonomía para uso continuo con indicador recargable."
  }
];

const idealUses = [
  {
    title: "Almacenes",
    text: "Pesaje funcional para control interno y movimientos de mercancía."
  },
  {
    title: "Recibo de mercancía",
    text: "Verifica entradas de producto sin complicar la operación diaria."
  },
  {
    title: "Recicladoras",
    text: "Pesaje básico para materiales y control interno no intensivo."
  },
  {
    title: "Pequeños negocios de distribución",
    text: "Apoyo para entradas, salidas y carga controlada."
  },
  {
    title: "Embarques ligeros",
    text: "Apoyo para revisar carga antes de salida o entrega."
  },
  {
    title: "Control de peso no intensivo",
    text: "Procesos internos con uso moderado y ambiente controlado."
  }
];

const platformSpecs = [
  ["Tipo de producto", "Báscula de Bajo Perfil"],
  ["Línea", "Base"],
  ["Modelo", "BAPER-12"],
  ["Capacidad máxima", "1,500 kg"],
  ["Dimensiones de plataforma", "120 × 120 cm"],
  ["Altura", "11 cm"],
  ["Material de fabricación", "Acero al carbón"],
  ["Superficie", "Antiderrapante"],
  ["Diseño", "Bajo perfil"],
  ["Cable de conexión", "5 metros"],
  ["Compatible con rampa", "Sí"],
  ["Celdas de carga", "4 celdas tipo Shearbeam en acero al carbón"],
  ["Uso recomendado", "Almacén, recibo y uso moderado"]
];

const indicatorSpecs = [
  ["Indicador de peso", "IPES"],
  ["Línea del indicador", "Comercial"],
  ["Tipo", "Solo peso"],
  ["Estructura", "Plástico"],
  ["Capacidad", "Programable"],
  ["Clase", "MEDIA III"],
  ["Alimentación", "Cargador 6V"],
  ["Pila", "Recargable, hasta 100 horas de uso continuo"],
  ["Fijación", "Sin soporte para mesa o pared"],
  ["Conectividad", "N/A"]
];

const goodFit = [
  "Necesitas una báscula funcional para almacén o recibo.",
  "Tu operación es moderada.",
  "Buscas una solución práctica y accesible.",
  "Trabajas con carga controlada.",
  "Necesitas una plataforma de 120 × 120 cm.",
  "No necesitas conectividad a PC o impresora.",
  "No necesitas instalación en fosa."
];

const considerOther = [
  "Necesitas uso intensivo durante varios turnos.",
  "Necesitas instalación en fosa.",
  "Requieres conectividad RS232.",
  "Trabajas en humedad, lavado o ambiente sanitario.",
  "Necesitas mayor capacidad.",
  "Requieres una configuración más robusta."
];

const faqs = [
  {
    question: "¿Qué capacidad tiene la Báscula de Bajo Perfil Base BAPER-12?",
    answer: "Tiene capacidad máxima de 1,500 kg."
  },
  {
    question: "¿Qué medida tiene la plataforma?",
    answer: "La plataforma mide 120 × 120 cm."
  },
  {
    question: "¿Qué altura tiene la báscula?",
    answer: "Tiene una altura aproximada de 11 cm."
  },
  {
    question: "¿De qué material está fabricada?",
    answer: "Está fabricada en acero al carbón con superficie antiderrapante."
  },
  {
    question: "¿Incluye indicador de peso?",
    answer: "Sí, incluye indicador IPES de línea comercial, tipo solo peso."
  },
  {
    question: "¿Tiene pila recargable?",
    answer: "Sí, el indicador cuenta con pila recargable de hasta 100 horas de uso continuo."
  },
  {
    question: "¿Se le puede colocar rampa?",
    answer: "Sí, es compatible con rampa."
  },
  {
    question: "¿Sirve para fosa?",
    answer:
      "No es la opción recomendada para instalación en fosa. Para ese caso conviene revisar otras líneas."
  },
  {
    question: "¿Tiene conexión a PC o impresora?",
    answer:
      "No, la línea Base no incluye conectividad. Si se requiere conexión a PC o impresor térmico, conviene revisar líneas superiores."
  },
  {
    question: "¿Hacen entrega en CDMX y Estado de México?",
    answer: "Sí, la entrega en CDMX y Estado de México se realiza sin cargo adicional."
  }
];

const productSchema = {
  "@context": "https://schema.org",
  "@type": "Product",
  name: "Báscula de Bajo Perfil Base BAPER-12",
  description: seo.description,
  brand: {
    "@type": "Brand",
    name: "Astil Básculas"
  },
  category: "Báscula industrial de piso",
  image: new URL("/Bajo-Perfil/base.webp", Astro.site ?? Astro.url).toString(),
  url: new URL("/basculas/bajo-perfil/base/", Astro.site ?? Astro.url).toString(),
  additionalProperty: [
    { "@type": "PropertyValue", name: "Modelo", value: "BAPER-12" },
    { "@type": "PropertyValue", name: "Medida", value: "120 × 120 cm" },
    { "@type": "PropertyValue", name: "Capacidad", value: "1,500 kg" },
    { "@type": "PropertyValue", name: "Altura", value: "11 cm" },
    { "@type": "PropertyValue", name: "Material", value: "Acero al carbón" },
    { "@type": "PropertyValue", name: "Superficie", value: "Antiderrapante" },
    { "@type": "PropertyValue", name: "Indicador", value: "IPES, comercial, solo peso" },
    { "@type": "PropertyValue", name: "Pila", value: "Recargable, hasta 100 horas" }
  ],
  offers: {
    "@type": "Offer",
    priceCurrency: "MXN",
    availability: "https://schema.org/InStock",
    areaServed: ["CDMX", "Estado de México"],
    seller: {
      "@type": "LocalBusiness",
      name: "Astil Básculas",
      telephone: "+52 56 1186 2291",
      email: contact.email
    }
  }
};

const faqSchema = {
  "@context": "https://schema.org",
  "@type": "FAQPage",
  mainEntity: faqs.map((faq) => ({
    "@type": "Question",
    name: faq.question,
    acceptedAnswer: {
      "@type": "Answer",
      text: faq.answer
    }
  }))
};

const breadcrumbSchema = {
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  itemListElement: [
    {
      "@type": "ListItem",
      position: 1,
      name: "Inicio",
      item: new URL("/", Astro.site ?? Astro.url).toString()
    },
    {
      "@type": "ListItem",
      position: 2,
      name: "Báscula de Bajo Perfil Base BAPER-12",
      item: new URL("/basculas/bajo-perfil/base/", Astro.site ?? Astro.url).toString()
    }
  ]
};
---

<BaseLayout
  title={seo.title}
  description={seo.description}
  keywords={seo.keywords}
  image="/Bajo-Perfil/base.webp"
  canonical="/basculas/bajo-perfil/base/"
  footerContactHref="#cotizar-base"
>
  <script type="application/ld+json" set:html={JSON.stringify(productSchema)} />
  <script type="application/ld+json" set:html={JSON.stringify(faqSchema)} />
  <script type="application/ld+json" set:html={JSON.stringify(breadcrumbSchema)} />

  <section class="base-hero">
    <div class="base-hero__inner">
      <div class="base-hero__copy">
        <p class="eyebrow">Báscula de Bajo Perfil Base · BAPER-12</p>
        <h1>Báscula de Bajo Perfil Base 120 × 120 cm</h1>
        <p class="base-hero__subheadline">
          Capacidad de 1,500 kg para almacén, recibo y operación moderada
        </p>
        <p class="lead">
          Una solución funcional para pesar mercancía, tarimas y carga controlada en procesos de
          almacén, recibo, recicladoras y pequeños negocios de distribución.
        </p>
        <div class="base-facts" aria-label="Datos rápidos de la báscula">
          {quickFacts.map((fact, index) => (
            <article>
              <span class="base-fact-icon" aria-hidden="true">
                {index === 0 && (
                  <svg viewBox="0 0 24 24" fill="none">
                    <path d="M12 4v3" />
                    <path d="M7 7h10" />
                    <path d="M8 7l-3 6h6L8 7Z" />
                    <path d="M16 7l-3 6h6l-3-6Z" />
                    <path d="M12 7v12" />
                    <path d="M8 20h8" />
                  </svg>
                )}
                {index === 1 && (
                  <svg viewBox="0 0 24 24" fill="none">
                    <rect x="5" y="5" width="14" height="14" rx="1.5" />
                    <path d="M9 3h6" />
                    <path d="M9 21h6" />
                    <path d="M3 9v6" />
                    <path d="M21 9v6" />
                  </svg>
                )}
                {index === 2 && (
                  <svg viewBox="0 0 24 24" fill="none">
                    <path d="M12 4v16" />
                    <path d="m8 8 4-4 4 4" />
                    <path d="m8 16 4 4 4-4" />
                    <path d="M5 4h4" />
                    <path d="M5 20h4" />
                  </svg>
                )}
                {index === 3 && (
                  <svg viewBox="0 0 24 24" fill="none">
                    <path d="M12 3 5 6v6c0 4.4 2.8 7.2 7 9 4.2-1.8 7-4.6 7-9V6l-7-3Z" />
                    <path d="M9 12h6" />
                    <path d="M10 15h4" />
                  </svg>
                )}
                {index === 4 && (
                  <svg viewBox="0 0 24 24" fill="none">
                    <path d="M4 18h16" />
                    <path d="M5 18 17 8h2v10" />
                    <path d="M8 18v-3" />
                    <path d="M12 18v-6" />
                  </svg>
                )}
                {index === 5 && (
                  <svg viewBox="0 0 24 24" fill="none">
                    <rect x="4" y="7" width="15" height="10" rx="2" />
                    <path d="M21 11v2" />
                    <path d="m10 15 2-3H9l2-3" />
                  </svg>
                )}
              </span>
              <div>
                <strong>{fact.value}</strong>
                <span>{fact.label}</span>
              </div>
            </article>
          ))}
        </div>
        <div class="button-row">
          <a class="button" href={whatsappHref}>Solicita cotización por WhatsApp</a>
          <a class="button secondary base-button--light" href={fichaHref} download>
            Descargar ficha técnica
          </a>
        </div>
        <p class="base-microcopy">Entregas en CDMX y Estado de México sin cargo adicional.</p>
      </div>
      <div class="base-hero__visual" aria-label="Composición visual de Báscula de Bajo Perfil Base">
        <div class="base-hero-product">
          <SmartImage
            name={imageSlots.hero}
            fallbackExt="webp"
            alt="Báscula de Bajo Perfil Base BAPER-12 de 120 x 120 cm"
            loading="eager"
            width="1600"
            height="900"
          />
        </div>
      </div>
    </div>
  </section>

  <section class="base-trust" aria-label="Beneficios rápidos">
    <div class="section-inner base-benefits-layout">
      <div class="base-benefits-media">
        <div class="base-trust__heading">
          <p class="eyebrow">Beneficios</p>
          <h2>Por qué elegir la Báscula Base</h2>
          <p>
            Una solución práctica para operaciones de pesaje básico que necesitan funcionalidad,
            seguridad y uso sencillo en almacén, recibo o carga controlada.
          </p>
        </div>
        <div class="base-media-card">
          <SmartImage
            name={imageSlots.surfaceDetail}
            fallbackExt="webp"
            alt="Detalle visual de superficie antiderrapante para báscula de bajo perfil"
            width="1200"
            height="800"
          />
        </div>
        <a class="button" href={whatsappHref}>Solicita cotización por WhatsApp</a>
      </div>
      <div class="base-trust__grid">
        {benefitCards.map((item, index) => (
          <article>
            <span class="base-benefit-icon" aria-hidden="true">
              {index === 0 && (
                <svg viewBox="0 0 24 24" fill="none">
                  <path d="M4 17h16" />
                  <path d="M6 17v-5h12v5" />
                  <path d="M8 12V9h8v3" />
                </svg>
              )}
              {index === 1 && (
                <svg viewBox="0 0 24 24" fill="none">
                  <path d="M12 3 5 6v6c0 4.4 2.8 7.2 7 9 4.2-1.8 7-4.6 7-9V6l-7-3Z" />
                  <path d="m9 12 2 2 4-5" />
                </svg>
              )}
              {index === 2 && (
                <svg viewBox="0 0 24 24" fill="none">
                  <path d="M12 4v3" />
                  <path d="M7 7h10" />
                  <path d="M8 7l-3 6h6L8 7Z" />
                  <path d="M16 7l-3 6h6l-3-6Z" />
                  <path d="M12 7v12" />
                </svg>
              )}
              {index === 3 && (
                <svg viewBox="0 0 24 24" fill="none">
                  <path d="M4 18h16" />
                  <path d="M5 18 17 8h2v10" />
                  <path d="m13 8 4-4 4 4" />
                </svg>
              )}
              {index === 4 && (
                <svg viewBox="0 0 24 24" fill="none">
                  <rect x="5" y="4" width="14" height="14" rx="2" />
                  <path d="M8 9h8" />
                  <path d="M9 14h6" />
                  <path d="M10 21h4" />
                </svg>
              )}
              {index === 5 && (
                <svg viewBox="0 0 24 24" fill="none">
                  <rect x="4" y="7" width="15" height="10" rx="2" />
                  <path d="M21 11v2" />
                  <path d="m10 15 2-3H9l2-3" />
                </svg>
              )}
            </span>
            <span class="base-benefit-badge">{item.badge}</span>
            <h3>{item.title}</h3>
            <p>{item.text}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <section class="section base-section">
    <div class="section-inner base-split-heading base-use-heading">
      <div>
        <p class="eyebrow">Uso ideal</p>
        <h2>Ideal para operaciones de pesaje básico y uso moderado</h2>
      </div>
      <p>
        La Báscula BAPER-12 es recomendable cuando necesitas pesar mercancía, productos o
        materiales en procesos internos de control, sin requerir conectividad avanzada o instalación
        en fosa.
      </p>
    </div>
    <div class="section-inner base-application-media">
      <SmartImage
        name={imageSlots.useIdeal}
        fallbackExt="webp"
        alt="Aplicación de báscula de bajo perfil en almacén y recibo"
        width="1600"
        height="1000"
      />
    </div>
    <div class="section-inner base-use-grid">
      {idealUses.map((item) => (
        <article>
          <span class="base-icon" aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none">
              <path d="M4 8h16v10H4z" />
              <path d="M7 8V5h10v3" />
              <path d="M8 13h8" />
            </svg>
          </span>
          <h3>{item.title}</h3>
          <p>{item.text}</p>
        </article>
      ))}
    </div>
    <div class="section-inner base-use-cta">
      <a class="button" href={whatsappHref}>Solicita cotización por WhatsApp</a>
    </div>
  </section>

  <section class="section base-includes-section">
    <div class="section-inner base-includes-shell">
      <div class="base-includes-head">
        <p class="eyebrow">Producto y configuración incluida</p>
        <h2>Producto y configuración incluida</h2>
        <p>
          La Báscula BAPER-12 integra plataforma de bajo perfil, indicador IPES y componentes
          principales para operación de pesaje básico.
        </p>
      </div>
      <div class="base-includes-summary-wrap">
        <div class="base-includes-summary">
          <strong>Configuración oficial del equipo</strong>
          <ul>
            <li>
              <strong>120 × 120 cm</strong>
              <span>Plataforma</span>
            </li>
            <li>
              <strong>1,500 kg</strong>
              <span>Capacidad</span>
            </li>
            <li>
              <strong>IPES</strong>
              <span>Indicador incluido</span>
            </li>
            <li>
              <strong>Hasta 100 horas</strong>
              <span>Pila recargable</span>
            </li>
          </ul>
        </div>
        <div class="button-row">
          <a class="button" href={whatsappHref}>Solicita cotización por WhatsApp</a>
          <a class="button secondary" href={fichaHref} download>Descargar ficha técnica</a>
        </div>
      </div>
      <div class="base-includes-visual-stack">
        <div class="base-includes-visual-frame">
          <article class="base-includes-platform">
            <SmartImage
              name={imageSlots.heroPlatform}
              fallbackExt="webp"
              alt="Plataforma de bajo perfil BAPER-12"
              class="base-image-slot base-image-slot--platform"
              width="1400"
              height="1050"
            />
            <div class="base-includes-visual-copy">
              <strong>Plataforma / equipo</strong>
              <span>Base de bajo perfil con superficie antiderrapante y estructura para operación moderada.</span>
            </div>
          </article>
          <article class="base-includes-indicator">
            <SmartImage
              name={imageSlots.indicator}
              fallbackExt="webp"
              alt="Indicador IPES para la BAPER-12"
              class="base-image-slot base-image-slot--indicator"
              width="900"
              height="900"
            />
            <div class="base-includes-visual-copy">
              <strong>Indicador IPES</strong>
              <span>Indicador comercial de solo peso con pila recargable.</span>
            </div>
          </article>
        </div>
      </div>
    </div>
    <div class="section-inner base-includes-tech">
      <div class="base-split-heading base-includes-tech__heading">
        <div>
          <p class="eyebrow">Especificaciones técnicas</p>
          <h3>Plataforma e indicador digital</h3>
        </div>
        <p>
          Consulta las características de la plataforma y del indicador digital incluidos en esta
          configuración.
        </p>
      </div>
      <div class="base-spec-grid">
        <details class="base-spec-card">
          <summary>
            <div class="base-spec-card__heading">
              <span>01</span>
              <div>
                <h3>Plataforma / Equipo</h3>
                <p>Bajo perfil, acero al carbón y superficie antiderrapante.</p>
              </div>
            </div>
            <span class="base-spec-toggle">Ver especificaciones de plataforma</span>
          </summary>
          <dl class="base-spec-list">
            {platformSpecs.map(([label, value]) => (
              <div>
                <dt>{label}</dt>
                <dd>{value}</dd>
              </div>
            ))}
          </dl>
        </details>
        <details class="base-spec-card">
          <summary>
            <div class="base-spec-card__heading">
              <span>02</span>
              <div>
                <h3>Indicador digital</h3>
                <p>IPES comercial, solo peso, con pila recargable.</p>
              </div>
            </div>
            <span class="base-spec-toggle">Ver especificaciones del indicador</span>
          </summary>
          <dl class="base-spec-list">
            {indicatorSpecs.map(([label, value]) => (
              <div>
                <dt>{label}</dt>
                <dd>{value}</dd>
              </div>
            ))}
          </dl>
        </details>
      </div>
    </div>
  </section>

  <section class="section base-guidance">
    <div class="section-inner">
      <div class="base-guidance__heading">
        <p class="eyebrow">Elección correcta</p>
        <h2>Cuándo elegir la Báscula de Bajo Perfil Base</h2>
        <p>
          Esta línea es ideal si buscas una báscula funcional para almacén, recibo, carga controlada
          y operación de uso moderado.
        </p>
      </div>
      <div class="base-choice-grid">
        <article class="base-choice-grid__yes">
          <h3>Sí te conviene si</h3>
          <ul class="base-check-list">
            {goodFit.map((item) => <li>{item}</li>)}
          </ul>
        </article>
        <article class="base-choice-grid__muted">
          <h3>Considera otra línea si</h3>
          <ul class="base-check-list">
            {considerOther.map((item) => <li>{item}</li>)}
          </ul>
        </article>
      </div>
      <div class="button-row base-centered-action">
        <a class="button secondary" href={withBase("/basculas/bajo-perfil/#seleccion-bajo-perfil")}>
          Ayúdame a elegir la línea correcta
        </a>
      </div>
    </div>
  </section>

  <section class="base-delivery">
    <div class="section-inner base-delivery__inner">
      <div class="base-delivery__copy">
        <div class="base-delivery__icon" aria-hidden="true">
          <svg viewBox="0 0 24 24" fill="none">
            <path d="M12 21s7-6.1 7-12a7 7 0 0 0-14 0c0 5.9 7 12 7 12Z" />
            <path d="M12 12a3 3 0 1 0 0-6 3 3 0 0 0 0 6Z" />
          </svg>
        </div>
        <p class="eyebrow">Entrega local</p>
        <h2>Entrega en CDMX y Estado de México sin cargo adicional</h2>
        <p>
          Recibe tu Báscula BAPER-12 directamente en tu empresa, almacén, comercio o zona de
          operación. También podemos orientarte sobre ubicación, rampas y condiciones recomendadas
          antes de instalar el equipo.
        </p>
        <a class="button" href={whatsappHref}>Solicita cotización por WhatsApp</a>
      </div>
      <div class="base-delivery__visual">
        <SmartImage
          name={imageSlots.application}
          fallbackExt="webp"
          alt="Entrega y operación local de báscula de piso en CDMX y Estado de México"
          class="base-delivery__image"
          pictureClass="base-delivery__picture"
          width="1200"
          height="750"
        />
      </div>
    </div>
  </section>

  <section class="section base-download">
    <div class="section-inner base-download__box">
      <div>
        <p class="eyebrow">Ficha técnica</p>
        <h2>Descarga la ficha técnica de la Báscula BAPER-12</h2>
        <p>
          Consulta la información técnica del equipo, medidas, capacidad, beneficios, aplicaciones y
          especificaciones del indicador antes de solicitar tu cotización.
        </p>
      </div>
      <div class="base-download__actions">
        <a class="button" href={fichaHref} download>Descargar ficha técnica PDF</a>
        <a class="button secondary" href={whatsappHref}>Solicita cotización por WhatsApp</a>
      </div>
    </div>
  </section>

  <section class="section base-faq">
    <div class="section-inner base-split-heading">
      <div>
        <p class="eyebrow">Preguntas frecuentes</p>
        <h2>FAQ de la Báscula de Bajo Perfil Base BAPER-12</h2>
      </div>
      <p>
        Respuestas rápidas para saber si esta báscula de piso en Estado de México y CDMX encaja con
        tu operación antes de cotizar.
      </p>
    </div>
    <div class="section-inner base-faq__list">
      {faqs.map((faq) => (
        <details>
          <summary>{faq.question}</summary>
          <p>{faq.answer}</p>
        </details>
      ))}
    </div>
  </section>

  <section class="base-final" id="cotizar-base">
    <div class="section-inner base-final__inner">
      <p class="eyebrow">Cotización</p>
      <h2>Cotiza tu Báscula de Bajo Perfil Base BAPER-12</h2>
      <p>
        Solicita información, confirma disponibilidad y recibe asesoría para saber si esta báscula es
        adecuada para tu operación.
      </p>
      <div class="button-row">
        <a class="button" href={whatsappHref}>Solicita cotización por WhatsApp</a>
        <a class="button secondary base-button--light" href={fichaHref} download>
          Descargar ficha técnica
        </a>
      </div>
      <p class="base-microcopy">Entrega en CDMX y Estado de México sin cargo adicional.</p>
    </div>
  </section>
</BaseLayout>

<style>
  .base-hero {
    padding: clamp(34px, 6vw, 74px) clamp(18px, 5vw, 72px);
    background:
      radial-gradient(circle at 78% 20%, rgba(47, 125, 246, 0.18), transparent 34%),
      linear-gradient(115deg, rgba(8, 17, 28, 0.99), rgba(18, 38, 58, 0.96)),
      repeating-linear-gradient(90deg, rgba(255, 255, 255, 0.04) 0 1px, transparent 1px 92px),
      #101820;
    color: #ffffff;
  }

  .base-hero__inner {
    display: grid;
    grid-template-columns: minmax(0, 0.74fr) minmax(560px, 1.08fr);
    gap: clamp(24px, 3.6vw, 52px);
    align-items: start;
    width: min(1340px, 100%);
    margin: 0 auto;
  }

  .base-hero h1 {
    max-width: 680px;
    margin-bottom: 12px;
    font-size: clamp(2.05rem, 4vw, 4.15rem);
    line-height: 0.98;
  }

  .base-hero__subheadline {
    max-width: 660px;
    margin: 0 0 16px;
    color: #ffffff;
    font-size: clamp(1.12rem, 2.1vw, 1.55rem);
    font-weight: 900;
    line-height: 1.2;
  }

  .base-hero .lead {
    max-width: 660px;
    color: #e8edf2;
    font-size: clamp(1rem, 1.55vw, 1.18rem);
    line-height: 1.55;
  }

  .base-facts {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 10px;
    margin-top: 24px;
  }

  .base-facts article {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 10px;
    align-items: center;
    min-height: 72px;
    padding: 12px;
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 8px;
    background:
      linear-gradient(135deg, rgba(255, 255, 255, 0.12), rgba(255, 255, 255, 0.045)),
      rgba(255, 255, 255, 0.06);
  }

  .base-fact-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    flex: 0 0 36px;
    border: 1px solid rgba(255, 255, 255, 0.16);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.08);
    color: rgba(255, 255, 255, 0.9);
  }

  .base-fact-icon svg {
    width: 20px;
    height: 20px;
    stroke: currentColor;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .base-facts strong {
    color: #ffffff;
    font-size: 1.06rem;
    font-weight: 950;
    line-height: 1.1;
  }

  .base-facts article div span {
    color: rgba(232, 237, 242, 0.78);
    font-size: 0.74rem;
    font-weight: 850;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .base-button--light {
    border-color: rgba(255, 255, 255, 0.48);
    background: transparent;
    color: #ffffff;
  }

  .base-hero .button-row .button:first-child {
    border: 1px solid rgba(255, 255, 255, 0.28);
    background: linear-gradient(180deg, #2f7df6, #0f5ed8);
    color: #ffffff;
    box-shadow:
      0 14px 34px rgba(47, 125, 246, 0.28),
      inset 0 1px 0 rgba(255, 255, 255, 0.18);
  }

  .base-hero .button-row .button:first-child:hover {
    border-color: rgba(255, 255, 255, 0.42);
    background: linear-gradient(180deg, #3b86ff, #1268ec);
  }

  .base-hero .button-row {
    display: flex;
    flex-wrap: nowrap;
    justify-content: start;
    gap: 10px;
  }

  .base-hero .button-row .button {
    width: auto;
    min-width: 0;
    padding: 12px 18px;
    white-space: nowrap;
  }

  .base-microcopy {
    margin: 12px 0 0;
    color: #dfe6ed;
    font-size: 0.94rem;
    font-weight: 760;
  }

  .base-hero__visual {
    position: relative;
    min-height: 0;
    align-self: start;
    transform: translate(clamp(-48px, -2.8vw, -28px), clamp(44px, 4vw, 74px));
  }

  .base-hero-product {
    position: relative;
    width: 100%;
    margin-left: auto;
    overflow: hidden;
    border-radius: 8px;
    transform: translateX(65px);
  }

  .base-hero-product img {
    display: block;
    width: 100%;
    aspect-ratio: 16 / 9;
    height: clamp(390px, 34vw, 560px);
    min-height: 0;
    border-radius: 8px;
    object-fit: cover;
    object-position: center 52%;
    box-shadow: 0 34px 88px rgba(0, 0, 0, 0.34);
  }

  .base-trust {
    padding: clamp(54px, 7vw, 96px) clamp(18px, 5vw, 72px);
    background:
      linear-gradient(135deg, rgba(47, 125, 246, 0.1), transparent 42%),
      #f4f7fb;
    border-bottom: 1px solid rgba(174, 188, 204, 0.66);
  }

  .base-benefits-layout {
    display: grid;
    grid-template-columns: minmax(470px, 0.76fr) minmax(0, 0.9fr);
    gap: clamp(24px, 4vw, 50px);
    align-items: center;
  }

  .base-benefits-media {
    display: grid;
    gap: 14px;
    align-content: center;
  }

  .base-trust__heading {
    margin-bottom: 0;
  }

  .base-trust__heading h2 {
    max-width: 760px;
    margin-bottom: 12px;
    font-size: clamp(1.8rem, 3.2vw, 3.2rem);
  }

  .base-trust__heading p {
    margin: 0;
    color: var(--muted);
  }

  .base-media-card,
  .base-application-media {
    padding: 12px;
    border: 1px solid rgba(174, 188, 204, 0.66);
    border-radius: 8px;
    background: #ffffff;
    box-shadow: 0 18px 42px rgba(8, 17, 28, 0.08);
  }

  .base-media-card img,
  .base-application-media img {
    width: 100%;
    border-radius: 6px;
    object-fit: cover;
  }

  .base-media-card img {
    aspect-ratio: 16 / 11;
    height: auto;
    object-position: center center;
  }

  .base-trust__grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
  }

  .base-trust article,
  .base-use-grid article,
  .base-line-list article {
    border: 1px solid rgba(174, 188, 204, 0.72);
    border-radius: 8px;
    background: #ffffff;
  }

  .base-trust article {
    display: grid;
    gap: 10px;
    align-content: start;
    min-height: 190px;
    padding: clamp(16px, 2vw, 22px);
    box-shadow: 0 12px 30px rgba(8, 17, 28, 0.06);
  }

  .base-benefit-icon,
  .base-include-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 42px;
    height: 42px;
    border-radius: 8px;
    background: #eef4ff;
    color: var(--blue-dark);
  }

  .base-benefit-icon svg,
  .base-include-icon svg {
    width: 22px;
    height: 22px;
    stroke: currentColor;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .base-trust h3 {
    margin: 0;
    font-size: 1.04rem;
    line-height: 1.15;
  }

  .base-benefit-badge {
    width: fit-content;
    padding: 5px 9px;
    border-radius: 999px;
    background: #f4f7fb;
    color: var(--blue-dark);
    font-size: 0.76rem;
    font-weight: 950;
    line-height: 1;
  }

  .base-trust p,
  .base-use-grid p,
  .base-line-list p {
    margin: 0;
    color: var(--muted);
  }

  .base-section {
    background:
      linear-gradient(180deg, rgba(248, 250, 252, 0.94), rgba(255, 255, 255, 0.96)),
      #ffffff;
  }

  .base-section--compact {
    padding-top: clamp(48px, 6vw, 78px);
  }

  .base-split-heading {
    display: grid;
    grid-template-columns: minmax(0, 0.82fr) minmax(280px, 0.48fr);
    gap: clamp(18px, 4vw, 48px);
    align-items: end;
    margin-bottom: clamp(24px, 4vw, 40px);
  }

  .base-use-heading {
    align-items: center;
    margin-bottom: clamp(28px, 4.4vw, 48px);
  }

  .base-split-heading h2,
  .base-guidance h2,
  .base-delivery h2,
  .base-download h2,
  .base-final h2 {
    font-size: clamp(1.9rem, 4vw, 3.8rem);
  }

  .base-split-heading > p,
  .base-guidance__heading > p,
  .base-delivery p,
  .base-download p,
  .base-final p {
    margin: 0;
    color: var(--muted);
    font-size: 1.02rem;
  }

  .base-use-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 14px;
  }

  .base-use-grid article {
    display: grid;
    gap: 10px;
    padding: 18px;
    min-height: 170px;
    box-shadow: 0 14px 34px rgba(8, 17, 28, 0.05);
  }

  .base-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 42px;
    height: 42px;
    border-radius: 8px;
    background: #eef4ff;
    color: var(--blue-dark);
  }

  .base-icon svg,
  .base-delivery__icon svg {
    width: 22px;
    height: 22px;
    stroke: currentColor;
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
  }

  .base-use-grid h3 {
    margin: 0;
  }

  .base-application-media {
    margin-bottom: 16px;
  }

  .base-application-media img {
    height: clamp(360px, 34vw, 500px);
    max-height: none;
    object-position: center 78%;
  }

  .base-use-cta {
    margin-top: 18px;
  }

  .base-includes-section {
    background:
      linear-gradient(135deg, rgba(47, 125, 246, 0.08), transparent 42%),
      linear-gradient(180deg, #ffffff, #f4f7fb),
      #ffffff;
  }

  .base-includes-shell {
    display: grid;
    grid-template-columns: 1fr;
    gap: 22px;
    align-items: start;
    margin-bottom: 22px;
  }

  .base-includes-head {
    display: grid;
    gap: 14px;
    align-content: start;
    max-width: 860px;
  }

  .base-includes-head h2 {
    margin-bottom: 0;
    max-width: 760px;
    font-size: clamp(2rem, 3.6vw, 3.35rem);
  }

  .base-includes-head > p {
    max-width: 760px;
    margin: 0;
    color: var(--muted);
    font-size: 1.04rem;
  }

  .base-includes-summary-wrap {
    display: grid;
    gap: 14px;
    max-width: 980px;
  }

  .base-includes-summary {
    padding: 18px 18px 16px;
    border: 1px solid rgba(16, 24, 32, 0.12);
    border-left: 4px solid var(--red);
    border-radius: 8px;
    background: #ffffff;
    box-shadow: 0 14px 34px rgba(8, 17, 28, 0.06);
  }

  .base-includes-summary strong {
    display: block;
    margin-bottom: 10px;
    color: var(--ink);
    font-size: 1.06rem;
  }

  .base-includes-summary ul {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 10px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .base-includes-summary li {
    display: grid;
    gap: 4px;
    align-content: start;
    min-height: 82px;
    padding: 12px 12px 12px;
    border-radius: 8px;
    background: #f4f7fb;
  }

  .base-includes-summary li strong {
    color: var(--ink);
    font-size: 1rem;
    font-weight: 950;
    line-height: 1.1;
  }

  .base-includes-summary li span {
    color: #5a6a7b;
    font-size: 0.76rem;
    font-weight: 850;
    line-height: 1.2;
    text-transform: uppercase;
  }

  .base-includes-visual-stack {
    display: block;
  }

  .base-includes-visual-frame {
    display: grid;
    grid-template-columns: minmax(0, 3fr) minmax(0, 2fr);
    gap: 18px;
    align-items: stretch;
    padding: 0;
    border: 0;
    border-radius: 0;
    background: transparent;
    box-shadow: none;
  }

  .base-includes-platform,
  .base-includes-indicator {
    display: grid;
    grid-template-rows: minmax(0, 1fr) auto;
    gap: 12px;
    align-content: stretch;
    height: 100%;
    padding: 14px;
    border: 1px solid rgba(174, 188, 204, 0.66);
    border-radius: 8px;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(248, 250, 252, 0.98)),
      #ffffff;
    box-shadow: 0 18px 44px rgba(8, 17, 28, 0.08);
  }

  .base-includes-platform {
    align-self: start;
  }

  .base-image-slot {
    display: block;
    width: 100%;
    height: 100%;
    border-radius: 6px;
    border: 1px solid rgba(174, 188, 204, 0.72);
    object-fit: cover;
    background:
      linear-gradient(135deg, rgba(47, 125, 246, 0.08), rgba(16, 24, 32, 0.05)),
      linear-gradient(180deg, rgba(255, 255, 255, 0.95), rgba(244, 247, 251, 0.95));
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.65);
  }

  .base-image-slot--platform {
    min-height: 0;
  }

  .base-includes-indicator {
    align-self: start;
  }

  .base-image-slot--indicator {
    aspect-ratio: 3 / 2;
    min-height: 0;
  }

  .base-includes-visual-copy strong {
    display: block;
    color: var(--ink);
    font-size: 1.02rem;
  }

  .base-includes-visual-copy span {
    color: var(--muted);
    font-size: 0.84rem;
    font-weight: 850;
  }

  .base-includes-summary-wrap .button-row {
    margin-top: 0;
  }

  .base-includes-tech {
    margin-top: 28px;
  }

  .base-includes-tech__heading {
    margin-bottom: 18px;
  }

  .base-includes-tech__heading h3 {
    margin: 0;
    font-size: clamp(1.6rem, 3vw, 2.4rem);
    line-height: 1.02;
  }

  .base-spec-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 16px;
  }

  .base-spec-card {
    padding: clamp(18px, 3vw, 26px);
    border: 1px solid rgba(174, 188, 204, 0.74);
    border-radius: 8px;
    background: #ffffff;
    box-shadow: 0 18px 42px rgba(8, 17, 28, 0.07);
  }

  .base-spec-card summary {
    display: grid;
    gap: 14px;
    list-style: none;
    cursor: pointer;
  }

  .base-spec-card summary::-webkit-details-marker {
    display: none;
  }

  .base-spec-card[open] summary {
    margin-bottom: 18px;
  }

  .base-spec-card__heading {
    display: grid;
    grid-template-columns: auto 1fr;
    gap: 12px;
    align-items: start;
    padding-bottom: 16px;
    border-bottom: 1px solid rgba(174, 188, 204, 0.54);
  }

  .base-spec-card__heading > span {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 42px;
    height: 42px;
    border-radius: 999px;
    background: #101820;
    color: #ffffff;
    font-weight: 950;
  }

  .base-spec-card h3 {
    margin: 0 0 5px;
    font-size: 1.18rem;
  }

  .base-spec-card p {
    margin: 0;
    color: var(--muted);
  }

  .base-spec-toggle {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: fit-content;
    min-height: 42px;
    padding: 10px 14px;
    border: 1px solid rgba(16, 24, 32, 0.18);
    border-radius: 8px;
    color: var(--blue-dark);
    font-size: 0.86rem;
    font-weight: 950;
  }

  .base-spec-list {
    display: grid;
    gap: 8px;
    margin: 0;
  }

  .base-spec-list div {
    display: grid;
    grid-template-columns: minmax(130px, 0.5fr) minmax(0, 1fr);
    gap: 12px;
    align-items: start;
    padding: 11px 12px;
    border: 1px solid rgba(174, 188, 204, 0.42);
    border-radius: 8px;
    background: #f8fafc;
  }

  .base-spec-list dt {
    color: var(--ink);
    font-size: 0.8rem;
    font-weight: 950;
    line-height: 1.25;
  }

  .base-spec-list dd {
    margin: 0;
    color: #405063;
    font-weight: 760;
    line-height: 1.35;
  }

  .base-spec-cta {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 18px;
  }

  .base-check-list {
    display: grid;
    gap: 10px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .base-check-list li {
    position: relative;
    padding-left: 22px;
    color: #2f3d4c;
    font-weight: 760;
  }

  .base-check-list li::before {
    content: "";
    position: absolute;
    top: 0.68em;
    left: 0;
    width: 8px;
    height: 8px;
    border-radius: 999px;
    background: var(--red);
  }

  .base-guidance {
    background:
      linear-gradient(135deg, rgba(16, 24, 32, 0.96), rgba(27, 48, 69, 0.94)),
      #101820;
    color: #ffffff;
  }

  .base-guidance__heading {
    max-width: 860px;
    margin-bottom: 26px;
  }

  .base-guidance__heading > p {
    color: #dfe6ed;
  }

  .base-choice-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 14px;
  }

  .base-choice-grid article {
    padding: clamp(18px, 3vw, 26px);
    border: 1px solid rgba(255, 255, 255, 0.14);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.08);
  }

  .base-choice-grid__yes {
    background:
      linear-gradient(135deg, rgba(34, 197, 94, 0.18), rgba(255, 255, 255, 0.07)),
      rgba(255, 255, 255, 0.08);
  }

  .base-choice-grid__muted {
    background:
      linear-gradient(135deg, rgba(47, 125, 246, 0.12), rgba(255, 255, 255, 0.045)),
      rgba(255, 255, 255, 0.045);
  }

  .base-choice-grid h3 {
    color: #ffffff;
  }

  .base-choice-grid li {
    color: #edf3f8;
  }

  .base-centered-action {
    justify-content: center;
  }

  .base-delivery {
    padding: clamp(18px, 2.4vw, 30px) clamp(18px, 4vw, 56px);
    background:
      linear-gradient(135deg, rgba(212, 33, 46, 0.12), transparent 44%),
      #f4f7fb;
  }

  .base-delivery__inner {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(460px, 520px);
    gap: 34px;
    align-items: center;
    max-width: 1180px;
    margin: 0 auto;
    padding: 26px 24px 26px 38px;
    border: 1px solid rgba(174, 188, 204, 0.72);
    border-radius: 8px;
    background: #ffffff;
    box-shadow: 0 16px 36px rgba(8, 17, 28, 0.07);
  }

  .base-delivery__copy {
    min-width: 0;
    max-width: 620px;
  }

  .base-delivery__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 42px;
    height: 42px;
    border-radius: 8px;
    background: #101820;
    color: #ffffff;
  }

  .base-delivery__icon svg {
    width: 21px;
    height: 21px;
  }

  .base-delivery__visual {
    width: 100%;
    height: auto;
    aspect-ratio: 16 / 9;
    padding: 6px;
    border: 1px solid rgba(174, 188, 204, 0.66);
    border-radius: 8px;
    background:
      linear-gradient(135deg, rgba(47, 125, 246, 0.08), transparent 48%),
      #f8fafc;
    max-width: 520px;
    justify-self: end;
    overflow: hidden;
  }

  :global(.base-delivery__picture),
  :global(.base-delivery__image),
  .base-delivery__visual :global(picture),
  .base-delivery__visual :global(img) {
    display: block;
    width: 100% !important;
    height: 100% !important;
  }

  :global(.base-delivery__image),
  .base-delivery__visual :global(img) {
    border-radius: 6px;
    object-fit: cover;
    object-position: center;
  }

  .base-delivery h2,
  .base-final h2 {
    max-width: 860px;
  }

  .base-delivery h2 {
    font-size: clamp(1.45rem, 2.35vw, 2.35rem);
    line-height: 1.02;
    max-width: 500px;
    margin-bottom: 8px;
  }

  .base-delivery p {
    max-width: 540px;
    margin-bottom: 8px;
    font-size: 0.95rem;
  }

  .base-download {
    background: #ffffff;
  }

  .base-download__box {
    display: grid;
    grid-template-columns: minmax(0, 0.86fr) minmax(280px, 0.4fr);
    gap: 22px;
    align-items: center;
    padding: clamp(22px, 4vw, 34px);
    border: 1px solid rgba(174, 188, 204, 0.72);
    border-radius: 8px;
    background:
      linear-gradient(135deg, rgba(47, 125, 246, 0.1), transparent 48%),
      #f8fafc;
  }

  .base-download__actions {
    display: grid;
    gap: 10px;
  }

  .base-line-list {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 10px;
  }

  .base-line-list article {
    display: grid;
    gap: 6px;
    padding: 16px;
  }

  .base-line-list strong {
    color: var(--ink);
  }

  .base-faq {
    background:
      linear-gradient(180deg, #ffffff, #f4f7fb),
      #f4f7fb;
  }

  .base-faq__list {
    display: grid;
    gap: 10px;
  }

  .base-faq details {
    border: 1px solid rgba(174, 188, 204, 0.78);
    border-radius: 8px;
    background: #ffffff;
  }

  .base-faq summary {
    padding: 18px;
    color: var(--ink);
    font-weight: 900;
    cursor: pointer;
  }

  .base-faq details p {
    margin: 0;
    padding: 0 18px 18px;
    color: var(--muted);
  }

  .base-final {
    padding: clamp(58px, 8vw, 108px) clamp(18px, 5vw, 72px);
    background:
      linear-gradient(115deg, rgba(8, 17, 28, 0.98), rgba(26, 45, 66, 0.94)),
      #101820;
    color: #ffffff;
  }

  .base-final__inner {
    display: grid;
    justify-items: center;
    max-width: 1080px;
    text-align: center;
  }

  .base-final h2 {
    max-width: 980px;
    margin-right: auto;
    margin-left: auto;
    text-wrap: balance;
  }

  .base-final__inner p {
    max-width: 720px;
    margin-right: auto;
    margin-left: auto;
    color: #dfe6ed;
    text-align: center;
  }

  .base-final .button-row {
    justify-content: center;
    margin-top: 22px;
  }

  .base-final .button-row .button:first-child {
    border: 1px solid rgba(255, 255, 255, 0.28);
    border-radius: 10px;
    background: #2f7df6;
    box-shadow: 0 14px 30px rgba(47, 125, 246, 0.28);
  }

  @media (max-width: 980px) {
    .base-hero__inner,
    .base-benefits-layout,
    .base-includes-shell,
    .base-delivery__inner,
    .base-split-heading,
    .base-download__box {
      grid-template-columns: 1fr;
    }

    .base-hero__visual {
      min-height: 0;
      transform: none;
    }

    .base-hero-product {
      width: 100%;
      margin-left: 0;
    }

    .base-trust__grid,
    .base-spec-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 760px) {
    .base-hero {
      padding-top: 32px;
      overflow: hidden;
    }

    .base-hero__inner {
      gap: 16px;
    }

    .base-hero__copy {
      display: contents;
    }

    .base-hero__copy,
    .base-hero__visual {
      min-width: 0;
    }

    .base-hero .eyebrow {
      order: 1;
    }

    .base-hero h1 {
      order: 2;
      font-size: clamp(2rem, 10vw, 2.8rem);
      line-height: 1.04;
      overflow-wrap: anywhere;
    }

    .base-hero__subheadline {
      order: 3;
      font-size: 1.06rem;
    }

    .base-hero .lead {
      order: 4;
    }

    .base-hero__visual {
      order: 6;
      width: 100%;
      margin-top: 4px;
    }

    .base-hero-product {
      width: 100%;
      margin-left: 0;
      transform: none;
    }

    .base-hero-product img {
      aspect-ratio: 16 / 10;
      height: clamp(230px, 58vw, 360px);
      object-position: center 52%;
    }

    .base-facts {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
      order: 5;
      margin-top: 6px;
    }

    .base-facts article {
      grid-template-columns: 34px minmax(0, 1fr);
      gap: 8px;
      min-height: 78px;
      padding: 10px;
    }

    .base-fact-icon {
      width: 34px;
      height: 34px;
      flex-basis: 34px;
    }

    .base-fact-icon svg {
      width: 18px;
      height: 18px;
    }

    .base-facts strong {
      display: block;
      font-size: clamp(0.98rem, 4.4vw, 1.2rem);
      overflow-wrap: anywhere;
    }

    .base-facts article div {
      min-width: 0;
    }

    .base-facts article div span {
      display: block;
      font-size: 0.66rem;
      overflow-wrap: anywhere;
    }

    .base-hero .button-row,
    .base-benefits-media .button,
    .base-includes-summary-wrap .button-row,
    .base-download__actions,
    .base-final .button-row {
      display: grid;
      grid-template-columns: 1fr;
    }

    .base-hero .button-row {
      display: grid;
      grid-template-columns: 1fr;
      gap: 10px;
      order: 7;
      margin-top: 4px;
    }

    .base-hero .button,
    .base-benefits-media .button,
    .base-includes-copy .button,
    .base-download .button,
    .base-final .button {
      width: 100%;
    }

    .base-hero .button {
      width: 100%;
      min-width: 0;
      padding-right: 12px;
      padding-left: 12px;
      font-size: 0.9rem;
      white-space: normal;
    }

    .base-microcopy {
      order: 8;
      margin-top: 0;
    }

    .base-trust__grid,
    .base-use-grid,
    .base-spec-grid,
    .base-choice-grid {
      grid-template-columns: 1fr;
    }

    .base-trust article {
      min-height: 0;
    }

    .base-trust h3 {
      margin-top: 0;
    }

    .base-trust p {
      grid-column: auto;
    }

    .base-media-card img {
      aspect-ratio: 4 / 3;
      height: auto;
      object-position: center 58%;
    }

    .base-application-media img {
      height: clamp(280px, 64vw, 400px);
      object-position: center 80%;
    }

    .base-includes-summary ul {
      grid-template-columns: 1fr;
    }

    .base-includes-shell {
      gap: 16px;
    }

    .base-includes-head {
      max-width: 100%;
    }

    .base-includes-visual-frame {
      grid-template-columns: 1fr;
      gap: 14px;
    }

    .base-image-slot--platform {
      aspect-ratio: 4 / 3;
      min-height: 0;
    }

    .base-includes-indicator {
      width: 100%;
      justify-self: stretch;
    }

    .base-image-slot--indicator {
      aspect-ratio: 1 / 1;
      min-height: 0;
    }

    .base-includes-summary-wrap .button-row {
      grid-template-columns: 1fr;
    }

    .base-spec-list div {
      grid-template-columns: 1fr;
      gap: 4px;
      padding: 12px;
    }

    .base-spec-cta {
      display: grid;
      grid-template-columns: 1fr;
    }

    .base-spec-cta .button {
      width: 100%;
    }

    .base-use-grid article {
      min-height: 0;
    }

    .base-delivery__inner {
      grid-template-columns: 1fr;
    }

    .base-delivery__icon {
      width: 54px;
      height: 54px;
    }
  }
</style>
